apiVersion: v1
kind: Service
metadata:
  name: {{ include "secret-store.fullname" . }}
  labels:
    {{- include "secret-store.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "secret-store.selectorLabels" . | nindent 4 }}
